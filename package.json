{
  "name": "puppeteer-api",
  "version": "1.0.0",
  "description": "Headless browser API for extracting links",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx puppeteer install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^22.8.2"
  }
}
